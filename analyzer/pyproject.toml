[project]
name = "analyzer"
version = "0.0.0"
description = "Topology builder and anomaly detection service (stub — implemented in later phases)"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
